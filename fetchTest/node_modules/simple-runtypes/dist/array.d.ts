import { Runtype } from './runtype';
export declare const arrayRuntype: Runtype<unknown[]>;
/**
 * An array of a given type.
 *
 * Options:
 *
 *   minLength .. reject arrays shorter than that
 *   maxLength .. reject arrays longer than that
 */
export declare function array<A>(a: Runtype<A>, options?: {
    maxLength?: number;
    minLength?: number;
}): Runtype<A[]>;
