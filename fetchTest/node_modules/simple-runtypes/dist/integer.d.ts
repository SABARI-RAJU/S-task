import { Runtype } from './runtype';
export declare const integerRuntype: Runtype<number>;
/**
 * A Number that is a `isSafeInteger()`
 *
 * Options:
 *
 *   min .. reject numbers smaller than that
 *   max .. reject number larger than that
 */
export declare function integer(options?: {
    max?: number;
    min?: number;
}): Runtype<number>;
