import { Runtype } from './runtype';
/**
 * Build a new record runtype that contains some keys from the original
 */
export declare function pick<T, K extends keyof T>(original: Runtype<T>, ...keys: K[]): Runtype<Pick<T, K>>;
